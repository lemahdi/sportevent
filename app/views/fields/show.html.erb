<% provide(:title, print_field(@field)) %>

<div class="row">
  <div class="span6 offset1">
    <p>
      <strong><%= @field.print_field(@field) %></strong>
      <br />
      Lat: <%= @field.latitude %>
      <br />
      Lon: <%= @field.longitude %>
    </p>

    <%= gmaps({
      "map_options" => { "auto_zoom" => false, "zoom" => 15 },
      "markers"     => { "data" => @json }
    }) %>

  </div>
</div>
