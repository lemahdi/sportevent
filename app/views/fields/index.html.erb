<% provide(:title, 'Fields') %>

<h1><%= I18n.t(:all_fields, scope: 'custom.view.field') %></h1>

<div class="row">
  <div class="col-md-12">

    <div class="map_container">
      <div id="map" class="map_with_all_fields" data-no-turbolink></div>
    </div>

		<% if !current_user.nil? && current_user.admin? %>
      <br />
      <%= link_to I18n.t(:new, scope: 'custom.noun') , new_field_path, class: "btn btn-lg btn-primary" %>
    <% end %>

    <br/>
    <%= link_to I18n.t(:how_save_field, scope: 'custom.view.field'), help_path(anchor: "FAQ6") %>

  </div>
</div>
